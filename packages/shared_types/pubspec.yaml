name: shared_types
description: Shared model primitives for Benny Wallet.
version: 0.1.0
publish_to: none

environment:
  sdk: "^3.8.0"

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0

dev_dependencies:
  build_runner: ^2.6.0
  freezed: ^3.2.0
  json_serializable: ^6.11.0
